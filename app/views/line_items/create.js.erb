/* "cart" is the id of div. Display the instance value of @cart through JQuery's HTML method */
$("#cart").html("<%= escape_javascript render(@cart) %>")

// 同时增加 //= require jquery-ui 在 application.js 中以实现动画效果
$('#current_item').css({ 'background-color': "#88ff88" }).animate({ 'background-color': "#114411" }, 1000);